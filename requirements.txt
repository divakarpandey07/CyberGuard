# CyberGuard IDS v4 – Python Dependencies
# Install: pip install -r requirements.txt
# ALL TOOLS ARE FREE — No subscriptions needed

# ── Web Framework ─────────────────────────────────────────────────────────
flask>=2.3.0
flask-cors>=4.0.0
flask-socketio>=5.3.6
python-socketio>=5.10.0
python-engineio>=4.8.0
# NOTE: eventlet REMOVED — it conflicts with threading mode on Windows.
# Flask-SocketIO works perfectly with async_mode="threading" (our default).

# ── Config / Environment Variables ────────────────────────────────────────
python-dotenv>=1.0.0       # .env file support — store API keys safely

# ── Machine Learning ──────────────────────────────────────────────────────
scikit-learn>=1.3.0
xgboost>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0

# ── Packet Capture ────────────────────────────────────────────────────────
scapy>=2.5.0

# ── HTTP (for GeoIP + Telegram + AbuseIPDB) ───────────────────────────────
requests>=2.31.0

# ── Dataset Download (optional) ───────────────────────────────────────────
gdown>=4.7.0

# ── Online/Incremental Learning (OPTIONAL) ────────────────────────────────
# Uncomment below and set ONLINE_LEARNING_ENABLED=True in .env
# river>=0.21.0
